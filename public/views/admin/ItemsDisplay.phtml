<!-- //$token = $data['input']['input'][0]; -->
<?php if (isset((($_SESSION['user'])['role'])) && (!empty($_SESSION['user'])) && (($_SESSION['user'])['role']) === 'admin') : ?>


    <?php if ((isset($data['input']['valuesToDisplay'])) && !empty($data['input']['valuesToDisplay'])) : ?>
        <h2> Articles de type <?= htmlspecialchars($data['input']['valuesToDisplay'][0]["productName"]); ?></h2>
        <p> Dans la catégorie <?= htmlspecialchars($data['input']['valuesToDisplay'][0]["categoryName"]); ?></p>
        <table class='catalog'>
            <thead>
                <tr>
                    <th>Référence</th>
                    <th>Conditionnement</th>
                    <th>Prix unitaire</th>
                    <th>TVA</th>
                    <th>Stock</th>
                    <th>statut</th>
                </tr>
            </thead>

            <tbody>
                <?php foreach ($data['input']['valuesToDisplay'] as $key => $valeur) : ?>
                    <tr>
                        <td><?= htmlspecialchars($valeur["itemRef"]); ?></td>
                        <td><?= htmlspecialchars($valeur["pack"]); ?></td>
                        <td><?= htmlspecialchars($valeur["price"]); ?></td>
                        <td><?= htmlspecialchars($valeur["name"]); ?></td>
                        <td><?= htmlspecialchars($valeur["stock"]); ?></td>
                        <td><?= htmlspecialchars($valeur["status"]); ?>
                        <td>
                        <td><a class="bouton" href="?route=Items&action=modifyItem&id=<?= $valeur['id_item']; ?>">Modifier</a></td>
                    </tr>
                <?php endforeach; ?>
            </tbody>

        </table>
    <?php endif; ?>

    <?php if ((isset($data['input']['valuesToDisplay'])) && empty($data['input']['valuesToDisplay'])) : ?>
        <p> Il n'existe encore aucune reférence pour ce produit </p>
    <?php endif; ?>



    <div>
        <a class="bouton" href="?route=Items&action=createItem&id=<?= htmlspecialchars($data['input']['idProduct']); ?>">Créer une nouvelle référence </a>
    </div>

<?php else : ?>
    <h2> Affichage non autorisé </h2>
<?php endif; ?>